# O Baileys declara peer deps opcionais e pesados (sharp, jimp, audio-decode)
# que só servem para mídia. Instalar automaticamente quebra o build em
# ambientes sem toolchain nativo — e o projeto não usa nenhum deles.
legacy-peer-deps=true
